"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/IUser');
const Fixture = require('../../fixtures/IUserFixture');

describe('models:IUser', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
