"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/IUserController');

describe('controllers:IUserController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
