"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Ticket');
const Fixture = require('../../fixtures/TicketFixture');

describe('models:Ticket', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
