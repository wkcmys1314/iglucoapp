"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Jpushrec');
const Fixture = require('../../fixtures/JpushrecFixture');

describe('models:Jpushrec', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
