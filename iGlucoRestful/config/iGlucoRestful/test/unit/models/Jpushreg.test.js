"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Jpushreg');
const Fixture = require('../../fixtures/JpushregFixture');

describe('models:Jpushreg', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
