"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/JpushrecController');

describe('controllers:JpushrecController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
