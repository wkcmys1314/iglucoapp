"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/TicketController');

describe('controllers:TicketController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
