"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/JpushregController');

describe('controllers:JpushregController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
